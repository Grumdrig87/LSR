jQuery(document).ready(function($) {
  
  //slider

  const wheelSlide = new Swiper('[data-kompl]', {
    slidesPerView: 4,
    spaceBetween: 23,
    initialSlide : 1,
  });
  //adaptive
  if ($(window).width() < 994) {
    
  }

})